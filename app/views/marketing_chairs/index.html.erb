  <%= link_to("<< Back to List of Nominees", {:controller => 'nominees', :action => 'index'}, :class => 'back-link') %>

  <div class="marketing_chairs index">
    <h2>Marketing Chair Candidates</h2>

    <table class="listing" summary="MarketingCHair list">
      <tr class="header">
        <th>First Name </th>
        <th>Last Name </th>
      </tr>


      <% @marketing_chairs.each do |marketing_chair| %>
      
		      <tr>
		        <td><%= marketing_chair.first_name %></td>
		        <td><%= marketing_chair.last_name %></td>
		      </tr>
		      
      <% end %>
    </table>
  </div> 
